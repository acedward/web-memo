{
  "name": "web-memo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Experimental browser demo for authenticated Zswap memos (memo-v3). Not an official Midnight product.",
  "license": "Apache-2.0",
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack serve --mode development",
    "pages:dev": "npx wrangler pages dev",
    "pages:deploy": "npm run build && npx wrangler pages deploy --branch=production",
    "verify:vendor": "node scripts/verify-vendor.mjs",
    "test": "npm run verify && npm run build && node test/read-matrix.mjs && node test/create-e2e.mjs",
    "test:read": "node test/read-matrix.mjs",
    "verify:fixtures": "node scripts/verify-fixtures.mjs",
    "verify": "npm run verify:vendor && npm run verify:fixtures",
    "test:create": "node test/create-e2e.mjs",
    "acceptance": "./acceptance/run-gate.sh .acceptance-out",
    "acceptance:artifacts": "node test/create-artifacts.mjs .acceptance-out/artifacts/page"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^12.0.2",
    "html-webpack-plugin": "^5.6.4",
    "webpack": "^5.101.3",
    "webpack-cli": "^6.0.1",
    "webpack-dev-server": "^5.2.2",
    "wrangler": "^4.42.0"
  }
}
